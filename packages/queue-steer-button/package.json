{
	"name": "queue-steer-button",
	"description": "Queue & Steer composer buttons for DeepSeek Harness: redirect a running session without stopping — Queue delivers the new request at the next agent step after the observation; Steer aborts the current thinking step, keeps the partial output as an interrupted assistant message, and continues with the new request.",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"main": "lib/index.js",
	"exports": {
		".": "./lib/index.js",
		"./client": "./lib/client.js",
		"./package.json": "./package.json"
	},
	"dsh": {
		"client": {
			"platform": "web"
		}
	},
	"files": [
		"lib"
	],
	"license": "MIT",
	"peerDependencies": {
		"@deepseek-ai/dsh-client-runtime": "^0.1.1-rc.1",
		"@deepseek-ai/cordis": "^4.0.1",
		"react": "^18.2.0"
	}
}
